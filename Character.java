import org.code.theater.*;
import org.code.media.*;

  /**
   * Subclass that uses the BackgroundScene object as a basis.
   */
public class Character extends BackgroundScene {

  public Character(String fileName, int x, int y){
    super(fileName, x, y);
    
  }
  
}
